name: Diagnose fourth-generation hybrid shrink

on:
  push:
    paths:
      - 'tests/fourth-generation-hybrid-shrink-diagnostic.test.js'
      - '.github/workflows/diagnose-fourth-hybrid-shrink.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    strategy:
      fail-fast: false
      matrix:
        mare: ['スプリングスイーツ','フィットレオタード','エイスト','ミニミニデート']
    runs-on: ubuntu-latest
    env:
      MARE: ${{ matrix.mare }}
    steps:
      - uses: actions/checkout@v4
      - name: Static syntax
        run: node --check tests/fourth-generation-hybrid-shrink-diagnostic.test.js
      - name: Hybrid shrink diagnostic
        run: node tests/fourth-generation-hybrid-shrink-diagnostic.test.js
