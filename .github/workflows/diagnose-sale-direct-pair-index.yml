name: Diagnose compact direct-pair index

on:
  push:
    paths:
      - 'sale-planner-core.js'
      - 'breeding-core.js'
      - 'tests/sale-direct-pair-index-diagnostic.test.js'
      - '.github/workflows/diagnose-sale-direct-pair-index.yml'
      - 'data/theory-master.json'
      - 'data/stallions.json'
      - 'data/default-broodmares.json'
      - 'data/nitro-effects.json'
      - 'data/kotta-pairs.json'
      - 'data/elaborate-direct-exceptions.json'
      - 'data/planner-inheritance-validation.json'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Static syntax
        run: node --check tests/sale-direct-pair-index-diagnostic.test.js
      - name: Compact direct-pair index diagnostic
        run: node tests/sale-direct-pair-index-diagnostic.test.js
