name: Diagnose breed pair nitro unification

on:
  push:
    paths:
      - 'tests/breed-pair-nitro-unification-diagnostic.test.js'
      - '.github/workflows/diagnose-breed-pair-nitro-unification.yml'
      - 'breeding-core.js'
      - 'data/theory-master.json'
      - 'data/stallions.json'
      - 'data/nitro-effects.json'
      - 'data/kotta-pairs.json'
      - 'data/elaborate-direct-exceptions.json'
      - 'data/planner-inheritance-validation.json'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Static syntax
        run: node --check tests/breed-pair-nitro-unification-diagnostic.test.js
      - name: Pair nitro unification
        run: node tests/breed-pair-nitro-unification-diagnostic.test.js
