name: Diagnose breed first-sire future provenance

on:
  push:
    paths:
      - 'tests/breed-first-sire-future-provenance-diagnostic.test.js'
      - '.github/workflows/diagnose-breed-first-sire-future-provenance.yml'
      - 'sale-planner-core.js'
      - 'sale-recommendation-core.js'
      - 'breeding-core.js'
      - 'data/theory-master.json'
      - 'data/stallions.json'
      - 'data/default-broodmares.json'
      - 'data/nitro-effects.json'
      - 'data/kotta-pairs.json'
      - 'data/elaborate-direct-exceptions.json'
      - 'data/planner-inheritance-validation.json'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Static syntax
        run: node --check tests/breed-first-sire-future-provenance-diagnostic.test.js
      - name: First-sire future provenance
        run: node tests/breed-first-sire-future-provenance-diagnostic.test.js
