name: Benchmark fourth-generation collector cost

on:
  push:
    branches:
      - candidate/sale-mare-potential-ui
    paths:
      - '.github/workflows/benchmark-breed-fourth-core-cost.yml'
      - 'tests/breed-fourth-collector-cost-diagnostic.test.js'
      - 'sale-planner-core.js'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Static syntax
        run: node --check tests/breed-fourth-collector-cost-diagnostic.test.js
      - name: Fourth-generation collector cost
        run: node tests/breed-fourth-collector-cost-diagnostic.test.js
