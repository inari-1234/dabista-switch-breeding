name: Benchmark third-generation bridge collector cost

on:
  push:
    branches:
      - candidate/sale-mare-potential-ui
    paths:
      - '.github/workflows/benchmark-breed-fourth-core-cost.yml'
      - 'tests/breed-third-bridge-collector-cost-diagnostic.test.js'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Static syntax
        run: node --check tests/breed-third-bridge-collector-cost-diagnostic.test.js
      - name: Third-generation bridge collector cost
        run: node tests/breed-third-bridge-collector-cost-diagnostic.test.js
