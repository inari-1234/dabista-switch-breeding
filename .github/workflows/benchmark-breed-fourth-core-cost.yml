name: Benchmark fourth-generation core cost

on:
  push:
    branches:
      - candidate/sale-mare-potential-ui
    paths:
      - '.github/workflows/benchmark-breed-fourth-core-cost.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Static syntax
        run: node --check tests/breed-fourth-core-cost-diagnostic.test.js
      - name: Fourth-generation core cost
        run: node tests/breed-fourth-core-cost-diagnostic.test.js
