name: Diagnose fourth-generation bridge ranks

on:
  push:
    paths:
      - 'tests/fourth-generation-source-rank-diagnostic.test.js'
      - '.github/workflows/diagnose-fourth-bridge-ranks.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Static syntax
        run: node --check tests/fourth-generation-source-rank-diagnostic.test.js
      - name: Fourth-generation bridge source-rank diagnostic
        run: node tests/fourth-generation-source-rank-diagnostic.test.js
